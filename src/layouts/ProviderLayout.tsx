import React from "react";
import EnterpriseLayout from "@/layouts/EnterpriseLayout";

export default function ProviderLayout() {
  return (
    <EnterpriseLayout
      role="provider"
      title="Provider Operations Workspace"
      subtitle="Catalog, fulfillment, payouts, notifications, and analytics in one role-focused shell."
    />
  );
}
