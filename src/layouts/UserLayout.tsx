import React from "react";
import EnterpriseLayout from "@/layouts/EnterpriseLayout";

export default function UserLayout() {
  return (
    <EnterpriseLayout
      role="user"
      title="User Experience Workspace"
      subtitle="Personalized activity, orders, tickets, saved content, and preference controls."
    />
  );
}
